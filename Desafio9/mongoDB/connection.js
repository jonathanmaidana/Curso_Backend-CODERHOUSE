const config = {
    mongodb: {
        conexionDB: 'mongodb://localhost:27017/mensajes',
        options: {
            useNewUrlParser: true,
            useUnifiedTopology: true
        }
    }
}

export default config